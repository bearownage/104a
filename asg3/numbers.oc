// $Id: 11-numbers.oc,v 1.7 2018-04-17 16:55:18-07 - - $

#include "oclib.h"

int main() {
   int number = 1;
   while (number > 0) {
      putint (number);
      putchar ('\n');
      number = number + number;
   }
   putint (number);
   putchar ('\n');
   return EXIT_SUCCESS;
}
