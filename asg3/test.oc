#include "oclib.h"


struct node {
   int foo;
   node link;
   string bar;
   string[] data;
}

node func (node head, int length) {
   int a = 0;
   a = node->bar;
   string b = "";
   //node->data = new string[foo];
   node c = new node;
   int[] d = 0;
   node->foo;
   d[5] = 5;
   b = "yes";
   func(yus, yus);
   if ( a < "hi" ) {
      a = length;
      c = c.link; 
   }
    //if ( 0 < foo ) {
    //  a = length;
    //}
 
     else { 
     if ( 0==0 ) b = 8;
          else a = "foo";
    }
}

bool hi (node[] length) {
   node[] ucsc = 10;
   return "yes";
}


