struct node {
   int foo;
   node link;
   string bar;
}

node func (node head, int length) {
   int a = 0;
   a = node->bar;
   string b = "";
   b[5];
   node c = new node;
   int[] d = 0;
   b = "yes";
   func(yus, yus);
   if ( a < "hi" ) {
      a = length;
      c = c.link; 
   }
    //if ( 0 < foo ) {
    //  a = length;
    //}
 
     else { 
     if ( 0==0 ) b = 8;
          else a = "foo";
    }
}

void hi (node[] length) {
   node[] ucsc = 10;
   return "yes";
}


